export const PHONE_NUMBER_LIST_STORAGE_NAME = 'phone-number-list-storage-name';
export const DESKTOP_URL = 'https://web.whatsapp.com/send?phone={phoneNumber}&text={message}';
export const MOBILE_URL = 'whatsapp://send?text={message}&phone={phoneNumber}';

const config = {
  PHONE_NUMBER_LIST_STORAGE_NAME,
  DESKTOP_URL,
  MOBILE_URL,
};

export default config;
